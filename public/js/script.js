/* Author: SCNEPTUNE */

$('.knob').knobKnob({
    snap : 20,          // Snap to zero if less than this deg.
    value: 90,         // Default rotation
    turn : function(ratio){
        ratio = 0.80;
    }
});
$('slider').slider();
T('mediastream')
console.log();























